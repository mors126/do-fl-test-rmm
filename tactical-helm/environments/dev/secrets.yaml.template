mongodb:
  auth:
    rootPassword: "${MONGO_ROOT_PASS}"
    username: "${MONGO_USER}"
    password: "${MONGO_PASS}"
    database: "${MONGO_DB}"

secrets:
  djangoSecretKey: "${DJANGO_SEC}"
  postgresUser: "${PG_USER}"
  postgresPassword: "${PG_PASS}"
  postgresDb: "${PG_DB}"
