name: Deploy Helm Chart to Minikube

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  STAGE: "dev"

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up minikube
        uses: helm/kind-action@v1.8.0
        with:
          cluster_name: kind

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.14.0

      - name: Display Kubernetes nodes (debug)
        run: kubectl get nodes

      - name: Create namespace (if needed)
        run: kubectl create namespace tactical || true

      - name: Deploy Helm chart
        run: |
          helm upgrade --install tactical ./tactical-helm \
            --namespace tactical \
            --create-namespace \
            --values ./tactical-helm/values.yaml \
            --values ./tactical-helm/environments/${{ env.STAGE }}/values.yaml \
            --set image.tag=1.1.1 \
            --set mongodb.auth.rootPassword=${{ secrets.MONGO_ROOT_PASS }} \
            --set mongodb.auth.username=${{ secrets.MONGO_USER }} \
            --set mongodb.auth.password=${{ secrets.MONGO_PASS }} \
            --set mongodb.auth.database=${{ secrets.MONGO_DB }} \
            --set secrets.djangoSecretKey=${{ secrets.DJANGO_SEC }} \
            --set secrets.postgresUser=${{ secrets.PG_USER }} \
            --set secrets.postgresPassword=${{ secrets.PG_PASS }} \
            --set secrets.postgresDb=${{ secrets.PG_DB }} \
            --wait --timeout 2m0s

      - name: Show deployed services
        run: kubectl get svc -n tactical
